# Grimoire — landing page

Landing bilíngue (en/pt-BR) do [Grimoire](../README.md), Next.js App Router estático.

```bash
npm install
npm run dev    # http://localhost:3000 (redireciona para /en ou /pt)
npm run build
```

Conteúdo em `dictionaries/{en,pt}.json`; rotas em `app/[lang]/`.
